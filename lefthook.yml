pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{js,ts,jsx,tsx,json}"
      run: npx biome check --write --staged .

    - name: typecheck
      run: npx tsc -b --noEmit

    - name: arch
      run: npm run arch:check
